# skip the startup message
startup_message off

# start with visual bell as default
vbell on
vbell_msg "bell on %t (%n)"

# screen tabs starting from 1 instead of 0
bind c screen 1
bind ^c screen 1
bind 0 select 10                                                            
screen 1
